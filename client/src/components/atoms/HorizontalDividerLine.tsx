import React, { FunctionComponent } from "react";

const HorizontalDividerLine: FunctionComponent<Record<string, never>> = () => {
    return <div className="horizontal-divider-line" />;
};

export default HorizontalDividerLine;
